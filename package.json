{
  "name": "learn-ts-decorators",
  "version": "1.0.0",
  "description": "Implement loggers via Typescript Decorators.",
  "scripts": {
    "start": "node dist",
    "build": "node scripts/clean.mjs && tsc",
    "dev": "nodemon src/index.ts --watch src --ext ts,graphql",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "winston": "^3.12.0"
  }
}
